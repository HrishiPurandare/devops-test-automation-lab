# DevOps Test Automation Lab

Purpose:
- Docker + CI experimentation repo for automation.
- Demonstrates Dockerized Playwright and PyTest runs, multi-job CI, and artifact handling.

Quickstart:
1. Build: `docker-compose build`
2. Run both suites: `docker-compose up --abort-on-container-exit`
3. CI file demonstrates multi-job orchestration.
